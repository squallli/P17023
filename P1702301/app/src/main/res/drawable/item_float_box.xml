<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!--conrners是設定圓角，radius是4個角統一的弧度半徑
        padding是填充域，這裡不是設置shape的填充域，而是設置裡面content的填充域
        solid是顏色，填充shape域的顏色，stroke是畫筆，類似於paint，這裡設置線粗和顏色-->
    <corners android:radius="7dp" />
    <padding android:left="5dp" android:top="5dp" android:right="5dp"
        android:bottom="5dp" />
    <solid android:color="#ffffff" />
    <stroke
        android:width="3dp"
        android:color="@color/colorEVAGreen" />

</shape>